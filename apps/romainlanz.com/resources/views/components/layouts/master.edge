<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{ title ?? 'RomainLanz' }}</title>

  {{-- Compiled Assets --}}
  @vite(['resources/js/app.js'])
</head>
<body {{{ page ? `data-page="${page}"` : "" }}}>
  {{{ await $slots.main() }}}
</body>
</html>
